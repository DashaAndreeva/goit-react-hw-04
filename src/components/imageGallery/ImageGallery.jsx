// import ImageCard from "./components/ImageCard/ImageCard";

// export default function ImageGallery(photos, ImageCard) {
//   return (
//     <ul>
//       {photos.map((photo) => (
//         <ImageCard key={photo.id} photo={photo} />
//       ))}
//     </ul>
//   );
// }
